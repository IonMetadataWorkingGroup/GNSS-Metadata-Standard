function CleanData( )

% delte the old files, to make sure that the 'TestConverter' execution
% actually creates some new files.
delete('L1E1bc.dat');
delete('L2L2C.dat');
delete('L5E5a.dat');












